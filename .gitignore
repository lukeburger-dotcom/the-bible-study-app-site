# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db

# editors
*.swp
*~

# tooling / local state
.claude/settings.local.json
node_modules/

# never
.env
.env.*
*.log

# SITE-COPY-1 §5: the comparison page was never published and is retired.
# Kept on disk, deliberately not tracked, so it stops reading as pending work.
compare.html
compare-card.png
